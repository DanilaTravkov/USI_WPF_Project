<UserControl x:Class="WPFTutorial.View.DirectorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFTutorial.View"
             xmlns:viewModel="clr-namespace:WPFTutorial.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>
        <StackPanel>
            <TextBlock Margin="5" Text="You are logged in as DIRECTOR"/>
            <StackPanel Orientation="Horizontal">
                <Button Width="150" Content="Create Exam" Command="{Binding ShowCreateExamCommand}" />
                <Button Width="150" Content="Assign teachers to exams" Command="{Binding ShowTeachersAssignedToExamsCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
